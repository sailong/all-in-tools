#!/usr/bin/env bash
# 示例变量文件（占位符） - 请在本地复制为 vars.sh 并填写真实值，或通过环境变量/CI secret 注入
export ACCOUNT_ID="your_account_id"
export TUNNEL_ID="your_tunnel_id"
export ACCOUNT_EMAIL="you@example.com"
export ACCOUNT_KEY="your_api_key"

# 注意：不要将真实凭证提交到版本控制；在 CI 中使用 Secret 管理凭证。
